@model IEnumerable<InventoryUi.Models.Menu>

<table class="table">
    <thead>
        <tr>
            <th>Name</th>
            <th>Url</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var product in Model)
        {
            <tr>
                <td>@product.Name</td>
                <td>@product.Url</td>
            </tr>
        }
    </tbody>
</table>
