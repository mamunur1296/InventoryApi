
@using System.Security.Claims
@using Microsoft.IdentityModel.JsonWebTokens

@{
    var userName = string.Empty;
    var userId = string.Empty;
    var fName = string.Empty;
    var userRole = string.Empty;
    var lName = string.Empty;
    var email = string.Empty;
    var img = string.Empty;

    if (User.Identity.IsAuthenticated)
    {
        var claimsIdentity = User.Identity as ClaimsIdentity;
        if (claimsIdentity != null)
        {
            // Extract the username from claims
            var userNameClaim = claimsIdentity.FindFirst(ClaimTypes.Name);
            if (userNameClaim != null)
            {
                userName = userNameClaim.Value;
            }

            // Extract the user ID from claims
            var userIdClaim = claimsIdentity.FindFirst("UserId") ?? claimsIdentity.FindFirst(ClaimTypes.NameIdentifier);
            if (userIdClaim != null)
            {
                userId = userIdClaim.Value;
            }

            // Extract the user role from claims
            var userRoleClaim = claimsIdentity.FindFirst(ClaimTypes.Role);
            if (userRoleClaim != null)
            {
                userRole = userRoleClaim.Value;
            }

            // Extract the first name from claims
            var fNameClaim = claimsIdentity.FindFirst("FName");
            if (fNameClaim != null)
            {
                fName = fNameClaim.Value;
            }

            // Extract the last name from claims
            var lNameClaim = claimsIdentity.FindFirst("LName");
            if (lNameClaim != null)
            {
                lName = lNameClaim.Value;
            }

            // Extract the email from claims
            var emailClaim = claimsIdentity.FindFirst("Email");
            if (emailClaim != null)
            {
                email = emailClaim.Value;
            }

            // Extract the image URL from claims
            var imgClaim = claimsIdentity.FindFirst("Img");
            if (imgClaim != null)
            {
                img = imgClaim.Value;
            }
        }

    }

}
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>@ViewData["Title"] - InventoryUi</title>
    <link rel="stylesheet" href="~/lib/bootstrap/dist/css/bootstrap.min.css" asp-append-version="true" />
    <link rel="stylesheet" href="~/css/site.css" asp-append-version="true" />
    <link rel="stylesheet" href="~/InventoryUi.styles.css" asp-append-version="true" />
    <!-- Toastr CSS -->
    <link href="~/lib/toastr.js/toastr.min.css" rel="stylesheet" asp-append-version="true" />
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css" asp-append-version="true">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css" asp-append-version="true">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons/font/bootstrap-icons.css" rel="stylesheet">
    <!-- Swiper CSS -->
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/swiper@10/swiper-bundle.min.css">
    
   
</head>
<body class="mx-auto" style="width: 90%;">
     
    <div class="shadow-sm">
        <nav class="navbar navbar-expand-lg bg-body-tertiary">
            <div class="container-fluid">
                <!-- Logo -->
                <a class="navbar-brand fs-2 fw-bolder text-black" href="#">LOGO</a>

                <!-- Toggler for mobile view -->
                <button class="navbar-toggler text-dark" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
                    <span class="navbar-toggler-icon"></span>
                </button>

                <!-- Navbar content -->
                <div class="collapse navbar-collapse justify-content-between" id="navbarSupportedContent">

                    <!-- Search form (centered and larger) -->
                    <form class="d-flex mx-auto w-75" role="search">
                        <input class="form-control ms-5 me-2 fs-5 fw-bold" type="search" placeholder="Search For Logo" aria-label="Search">
                        <button class="btn btn-dark text-white fw-bold fs-5" type="submit">Search</button>
                    </form>

                    <!-- Right side (Shopping cart and Sign In buttons) -->
                    <div>
                        <div class="d-flex align-items-center">
                            <!-- Shopping cart button with icon and tooltip -->
                            <a href="#" class="btn position-relative">
                                <!-- Shopping cart icon -->
                                <i class="fas fa-shopping-cart fs-4"></i>

                                <!-- Red badge indicating the number of items in the cart -->
                                <span class="position-absolute top-0 start-100 translate-middle badge rounded-pill bg-success border border-light">
                                    3 <!-- Change this number dynamically with JavaScript if needed -->
                                    <span class="visually-hidden">Cart items</span>
                                </span>
                            </a>


                            <!-- Sign-in button with icon -->
                            @{
                                if (User.Identity.IsAuthenticated)
                                {

                                    <div class="nav-item dropdown">
                                        <a class="nav-link   d-flex align-items-center" data-bs-toggle="dropdown" href="#" role="button" aria-expanded="false">
                                            <span class="btn">
                                                <img src="/images/User/@img"
                                                     alt="Profile Image"
                                                     class="rounded-circle mb-2"
                                                     width="40"
                                                     height="40"
                                                     onerror="this.onerror=null;this.src='/ProjectRootImg/default-user.png';">
                                            </span>
                                        </a>
                                        <ul class="dropdown-menu dropdown-menu-end">
                                            <li class="dropdown-header text-center">
                                                <img src="/images/User/@img"
                                                     alt="Profile Image"
                                                     class="rounded-circle mb-2"
                                                     width="60"
                                                     height="60"
                                                     onerror="this.onerror=null;this.src='/ProjectRootImg/default-user.png';">
                                                <span>@userName</span>
                                                <p class="dropdown-item-text">@fName @lName</p>
                                            </li>
                                            <li><hr class="dropdown-divider"></li>
                                            <li><a class="dropdown-item" asp-action="Profile" asp-controller="User" asp-route-id="@userId">My Profile</a></li>
                                            <li><a class="dropdown-item" asp-action="Index" asp-controller="Dashboard">Dashboard</a></li>
                                            <li><a class="dropdown-item" href="#">Need Help?</a></li>
                                            <li><hr class="dropdown-divider"></li>
                                            <li><a class="dropdown-item" asp-controller="Auth" asp-action="LogOut">Sign Out</a></li>
                                        </ul>
                                    </div>
                                }
                                else
                                {

                                    <a asp-action="Login" asp-controller="Auth" class="btn fs-5 text-primary fw-bold">
                                        <i class="fas fa-sign-in-alt"></i> Sign In
                                    </a>
                                }
                            }
                            
                        </div>
                    </div>
                </div>
            </div>
        </nav>
        <nav class="navbar navbar-expand-lg bg-body-tertiary">
            <div class="container-fluid">
                <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
                    <span class="navbar-toggler-icon"></span>
                </button>
                <div class="collapse navbar-collapse" id="navbarNav">
                    <ul class="navbar-nav">
                        <!-- Home Nav Item -->
                        <li class="nav-item me-3">
                            <a class="nav-link active fs-5 fw-bold" aria-current="page" asp-action="index" asp-controller="Home">
                                <i class="fas fa-home me-1"></i> <!-- Home Icon -->
                                Home
                            </a>
                        </li>

                        <!-- Shops Nav Item -->
                        <li class="nav-item me-3">
                            <a class="nav-link active fs-5 fw-bold" aria-current="page" asp-action="Shops" asp-controller="Home">
                                <i class="fas fa-store me-1"></i> <!-- Shops Icon -->
                                Shops
                            </a>
                        </li>

                        <!-- Categories Nav Item -->
                        <li class="nav-item me-3">
                            <a class="nav-link active fs-5 fw-bold" aria-current="page" href="#">
                                <i class="fas fa-th-large me-1"></i> <!-- Categories Icon -->
                                Categories
                            </a>
                        </li>
                        @{
                            if (User.Identity.IsAuthenticated)
                            {
                                <!-- Orders Nav Item -->
                                <li class="nav-item me-3">
                                    <a class="nav-link active fs-5 fw-bold" aria-current="page" href="#">
                                        <i class="fas fa-box me-1"></i> <!-- Orders Icon -->
                                        Orders
                                    </a>
                                </li>

                                <!-- My eStore Nav Item -->
                                <li class="nav-item me-3">
                                    <a class="nav-link active fs-5 fw-bold" aria-current="page" href="#">
                                        <i class="fas fa-store me-1"></i> <!-- eStore Icon -->
                                        My eStore
                                    </a>
                                </li>

                                <!-- Delivery Address Nav Item -->
                                <li class="nav-item me-3">
                                    <a class="nav-link active fs-5 fw-bold" aria-current="page" href="#">
                                        <i class="fas fa-map-marker-alt me-1"></i> <!-- Delivery Address Icon -->
                                        Delivery Address
                                    </a>
                                </li>

                                
                            }
                            
                        }
                    </ul>
                </div>

            </div>
        </nav>
    </div>

    <main role="main">
        @RenderBody()
    </main>
  
    <script src="~/lib/jquery/dist/jquery.min.js" asp-append-version="true"></script>
    <script src="~/lib/bootstrap/dist/js/bootstrap.bundle.min.js" asp-append-version="true"></script>
    <script src="~/js/site.js" asp-append-version="true"></script>
    <!-- Toastr JS -->
    <script src="~/lib/toastr.js/toastr.min.js" asp-append-version="true"></script>

    <script src="https://code.jquery.com/jquery-3.5.1.min.js" asp-append-version="true"></script>
    <script src="https://maxcdn.bootstrapcdn.com/bootstrap/4.5.2/js/bootstrap.min.js" asp-append-version="true"></script>
    <script src="https://kit.fontawesome.com/YOUR-KIT-ID.js" asp-append-version="true" crossorigin="anonymous"></script>
    <script src="https://cdn.jsdelivr.net/npm/swiper@10/swiper-bundle.min.js"></script>
    <script>
        // Function to update cart tooltip with item count
        function updateCartTooltip(itemCount) {
            var cartIcon = document.getElementById('cart-icon');
            cartIcon.setAttribute('title', itemCount + ' items in cart');
            var tooltip = bootstrap.Tooltip.getInstance(cartIcon);
            tooltip.setContent({ '.tooltip-inner': itemCount + ' items in cart' });
        }

        // Initialize the tooltip
        var tooltipTriggerList = [].slice.call(document.querySelectorAll('[data-bs-toggle="tooltip"]'))
        var tooltipList = tooltipTriggerList.map(function (tooltipTriggerEl) {
            return new bootstrap.Tooltip(tooltipTriggerEl)
        })

        // Example usage (update dynamically)
        updateCartTooltip(5);  // This will change the tooltip to "5 items in cart"
    </script>
    @await RenderSectionAsync("Scripts", required: false)
    

</body>
</html>
